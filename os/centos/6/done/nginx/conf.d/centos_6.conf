server {
    listen       80;
    server_name  gomarket.goapk.com;

    #charset koi8-r;
    access_log  /var/log/nginx/log/super.access.log  main;
    error_log  /var/log/nginx/log/super.error.log  debug;

    root /home/jimmychou/workspace/goapk/trunk/newgomarket.goapk.com;

    location / {
        index  index.php index.html index.htm;
    }

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }

    # proxy the PHP scripts to Apache listening on 127.0.0.1:80
    #
    #location ~ \.php$ {
    #    proxy_pass   http://127.0.0.1;
    #}

    # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
    #
    location ~ \.php$ {
        fastcgi_pass   127.0.0.1:9000;
        fastcgi_index  index.php;
        fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
        include        fastcgi_params;
    }

    # deny access to .htaccess files, if Apache's document root
    # concurs with nginx's one
    #
    #location ~ /\.ht {
    #    deny  all;
    #}
}

server {
    listen       80;
    server_name  super-local.1mobile.com;

    #charset koi8-r;
    access_log  /var/log/nginx/log/super.access.log  main;
    error_log  /var/log/nginx/log/super.error.log  debug;

    root /home/jimmychou/workspace/1mobile/super/webroot;

    location / {
        index  index.php index.html index.htm;
    }

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }

    # proxy the PHP scripts to Apache listening on 127.0.0.1:80
    #
    #location ~ \.php$ {
    #    proxy_pass   http://127.0.0.1;
    #}

    # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
    #
    location ~ \.php$ {
        fastcgi_pass   127.0.0.1:9000;
        fastcgi_index  index.php;
        fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
        include        fastcgi_params;
    }

    # deny access to .htaccess files, if Apache's document root
    # concurs with nginx's one
    #
    #location ~ /\.ht {
    #    deny  all;
    #}
}


#	nginx: [warn] the "log_format" directive may be used only on "http" level in /etc/nginx/conf.d/default.conf:60 

    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';
    log_format  my_format  'the remote_addr is $remote_addr - and the remote_user is $remote_user [the time_local is $time_local] "	and the request is $request" '
			' "	and the binary_remote_addr is $binary_remote_addr" "	and the body_bytes_sent is $body_bytes_sent" "	and the content_type is $content_type" "	and the content_length is $content_length" "	and the document_root is $document_root" "	and the document_uri is $document_uri" "	and the fastcgi_script_name is $fastcgi_script_name"	"	and the request_uri is $request_uri"	"	and the limit_rate is $limit_rate" "	and the nginx_version is $nginx_version" "	and the remote_addr is $remote_addr" "	and the remote_port is $remote_port" "	and the server_addr is $server_addr" "	and the server_name is $server_name" "	and the server_port is $server_port" "	and the server_protocol is $server_protocol" "	the scheme is $scheme	" "	the uri is $uri	" ';


server {
    listen       80;
    server_name  centos.jimmychou.com localtest 10.9.179.247;

    #charset koi8-r;
    #access_log  /var/log/nginx/log/localtest.access.log	my_format;
    #error_log  /var/log/nginx/log/localtest.error.log	crit;


    #rewrite ^/analytics$ crossdomain.xml;	#	no / ahead is wrong 
    rewrite ^/analytics$ /crossdomain.xml; 

    location / {
	root /home/jimmychou/workspace/jimmy/php;
        index  index.php index.html index.htm;
    }

    location /phpmyadmin {
        root   /usr/share/phpmyadmin;
        index  index.php index.html index.htm;
    }

    #location ~* /^phpmyadmin.*\.php$ {	#	fuck!
    location ~* ^/phpmyadmin.*\.php$ {
        root   /usr/share/phpmyadmin;
        fastcgi_pass   127.0.0.1:9000;
        fastcgi_index  index.php;
        fastcgi_param  SCRIPT_FILENAME  /usr/share/$fastcgi_script_name;
        include        fastcgi_params;
    }

    location ~* ^/phpmyadmin.*\.(gif|css|jpg|jpeg|png|js|bmp|ico)$ {
        root   /usr/share;
    }

    #error_page  404              /404.html;

    # redirect server error pages to the static page /50x.html
    #
    error_page   500 502 503 504  /50x.html;
    location = /50x.html {
        root   /usr/share/nginx/html;
    }

    # proxy the PHP scripts to Apache listening on 127.0.0.1:80
    #
    #location ~ \.php$ {
    #    proxy_pass   http://127.0.0.1;
    #}

    # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
    #
    location ~ \.php$ {
	root /home/jimmychou/workspace/jimmy/php;
        fastcgi_pass   127.0.0.1:9000;
        fastcgi_index  index.php;
        #fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
        fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
        include        fastcgi_params;
    }
    # deny access to .htaccess files, if Apache's document root
    # concurs with nginx's one
    #
    #location ~ /\.ht {
    #    deny  all;
    #}
}
